# External review triage: Patch 1

Date: 2026-07-26

Reviewed production candidate: `3778e1fae6e7e3d885252282a7c5ce67e06730ff`

Pending test branch: `review/code-mode-roundtable-test-polish`

At triage time, the pending test branch is still identical to the reviewed production candidate.

## Inputs

Two independent static reviews were received after publication of the external reviewer packet:

- one Codex review;
- one Claude review.

Neither external reviewer compiled or ran the candidate. Their findings are static-review inputs that must be confirmed by repository-native tests where applicable.

## Consolidated verdict

**Production correctness remains pass.**

Both reviewers independently confirmed the selected provenance path, manager-owned liveness authority, exact-cell lookup, terminal-only reporting, yielded neutrality, truncation placement, schema compatibility, and lifecycle non-expansion. Neither found a blocking production correctness defect.

**Publication remains blocked on the test-only revision and targeted compatibility validation.**

## Accepted test changes

The pending Agent 2 test-only revision should retain the roundtable scope and add the following external-review findings:

1. Remove the standalone integration target and register the five cases through the existing aggregate `all` test binary.
2. Reuse the existing code-mode suite helpers and assertion imports, including the suite's `pretty_assertions::assert_eq` convention.
3. Ensure process-creating turn submission is inside panic/error cleanup protection.
4. Replace the fixed survivor sleeps with a bounded or deterministic handshake.
5. Assert yielded neutrality by checking that `Background sessions still running:` is absent, rather than scanning all numeric tokens.
6. Avoid an exact content-item-count assertion in the large-output case when the behavioural contract only requires a separate non-empty emitted-output representation.
7. Add a direct manager-query unit test that covers exact creator-cell matching, `None` creator exclusion, exited-process exclusion, and deterministic ordering without network or Windows dependence.
8. Run the existing code-mode aggregate suite, or at minimum the two identified exposed tests, repeatedly on the candidate and exact upstream base. The relevant risk is a short-lived nested `exec_command` remaining manager-visible during terminal header formatting and colliding with existing anchored header assertions.

If the existing tests expose the optional summary race, update only the affected expectations to accept the correct point-in-time summary. Do not loosen unrelated header assertions.

No new broad project or workspace run is required unless the focused compatibility run reveals a persistent candidate-only failure.

## Production hardening question

The summary currently joins every exact-cell live session ID. The manager has a normal soft process cap, but the store can temporarily exceed that cap while locked exited entries are being finalized. Therefore the summary has no independent hard cardinality bound.

This is not a demonstrated correctness failure, and normal model-history limits remain downstream. However, repository guidance prefers a hard bound for every model-visible fragment. Before the final production head is frozen, Agent 1 and Agent 3 should choose one of two explicit positions:

1. add a deterministic display cap with an omitted-count marker; or
2. retain the full point-in-time list and document that it remains subject to normal global history truncation.

Do not change process pruning or lifecycle policy to solve this presentation-bound question.

## Standalone issue strategy

A separate upstream issue remains part of the publication plan.

Issue `openai/codex#34866` overlaps with one symptom and explicitly mentions generated JavaScript forwarding only `r.output` while omitting `r.session_id` or `r.exit_code`. That overlap should be acknowledged and cross-linked, but it does not require collapsing this investigation into that issue.

The planned issue has a materially different evidence and contract package:

- an executable negative reproduction built around deliberate loss of multiple live session handles;
- direct verification that the manager still owns the processes;
- exact creator-cell attribution and two-cell isolation;
- deterministic numeric ordering and exited-process exclusion;
- yielded neutrality and truncation-placement coverage;
- a visibility-only implementation with explicit lifecycle non-goals;
- a clean candidate, focused validation, matched-base differential, and independent review trail.

The issue should therefore stand on its own as the canonical problem statement for loss of model-visible control information when terminal code-mode rendering omits still-live nested session IDs. It should cross-link #34866 as related prior symptom coverage and explain the narrower, more testable contract rather than claiming there is no overlap.

Recommended publication sequence after all technical gates pass:

1. publish the standalone issue;
2. open the PR immediately afterward;
3. cross-link the issue and PR in the same working session;
4. reference #34866 and the other related reports in a concise related-work section without treating this issue as subordinate to them.

Do not replace the standalone issue with only a comment on #34866.

## Required public-copy corrections

- Use `stored live process entries`, not `already-persisted live process entries`; `ProcessEntry` is in-memory manager state, not rollout persistence.
- Use `session-level unified-exec process manager`, not `conversation-level process manager`.
- Use `session ID` in model-facing prose and reserve `process ID` for internal implementation discussion.
- Mark example numeric IDs as illustrative.
- State that the summary is outside code-mode emitted-output truncation but still subject to later global history limits.
- Describe the three unified-exec checks as existing focused lifecycle/regression tests updated and rerun, not newly added manager-query tests.
- State the platform/environment for acceptance results.
- State directly that the complete workspace suite was not run.
- Remove the teardown claim until the setup-error cleanup gap is fixed and revalidated.
- Fix the PR draft's five-cases/six-bullets mismatch and remove all unfilled placeholders before publication.
- Keep the issue problem-led while preserving the evidence that distinguishes it from related reports.

## Maintainer concerns to explain, not pre-emptively refactor

The following are credible review questions but do not currently justify production churn:

- unified exec storing the code-mode `CellId` type rather than a generic process-origin enum;
- the manager query mirroring the existing `list_processes` liveness predicate;
- the one-caller `with_creator_cell_id(Option<CellId>)` builder shape;
- duplicate sorting in the manager and formatter;
- no generic direct-source ownership test;
- no end-to-end `Terminated` acceptance case;
- the point-in-time over-report window for cached remote exit state.

The PR should explain the narrow-scope rationale and invite maintainer preference. Do not broaden Patch 1 into generic provenance, lifecycle, or recovery architecture without a concrete request.

## Gate status after triage

Open:

- Agent 2 publishes the test-only revision and focused receipt.
- Agent 2 or the validation owner runs the targeted aggregate code-mode compatibility check on candidate and base.
- Agent 3 reviews the revised test diff and compatibility evidence.
- Agent 1 and Agent 3 resolve the display-cardinality position.
- Agent 4 revises the standalone issue and PR drafts against the final candidate.
- The human coordinator approves final issue wording, PR wording, and issue-first publication sequencing.

No upstream issue, comment, or PR should be published before these gates close.