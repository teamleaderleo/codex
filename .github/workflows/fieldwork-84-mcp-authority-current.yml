name: fieldwork-84-mcp-authority-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Retired duplicate carrier. Authoritative execution is fieldwork-84-mcp-authority-execute.'
